# Status update — 2026-07-27

Repo: `khuffsphone/tinywest` · Branch: `claude/eager-dirac-qv4zf4` (HEAD `a4c4dd4`, clean).
No open PR exists for this branch yet.

## What's in the repo right now

| Path | Status |
| --- | --- |
| `index.html` | Sunset Riders tribute build (protected, untouched since `cfb0034`). |
| `slice/` — 42-second vertical slice of Rampage Express | Unchanged since `a4c4dd4` (2026-07-17 build). |
| `docs/tiny-west-brain/` | Local index + constants + reconciliation + build receipt, current. |

No commits have landed since the slice build (`a4c4dd4`). This cycle's work was **verification
only** — re-running the documented gates to confirm the artifact still holds, not new development.

## Verification re-run this cycle

Ran the full build/test chain from `CLAUDE.md` against the existing `slice/` source (playwright
installed locally and removed afterward; no repo files changed):

- `node tools/build.mjs` → `slice/dist/tiny-west.html`, **106,552 bytes**, byte-identical to the
  receipted artifact (SHA-256 `6580961c...0bba` per `BUILD_RECEIPT_SLICE.md`).
- `node tools/playtest.mjs` → smoke PASS, `runDeterminismCheck` PASS (hashA == hashB ==
  `911533983`), natural keyboard run to `results` in 27s sim time, **$269 banked** — matches the
  receipt's recorded run exactly. Zero page errors.
- `node tools/qa2.mjs` → touch pads + determinism on touch device PASS; four viewports
  (1100×760, 412×915, 360×640, 915×412) all `overflow:false`; 20 reset cycles bounded, zero
  errors.
- `node tools/qa3.mjs` → pay-car fork reachable and completable via exact injected input,
  **$839 banked** at route end — matches the receipt. Zero errors.

**Conclusion: no regressions.** The 42-second slice reproduces its audited build byte-for-byte
and behaviorally, seven+ days after the original build with no intervening commits.

## Standing open items (unchanged, carried from `RECONCILIATION.md` / `README.md`)

- `NEEDS_DECISION` (owner-level, still open): final public subtitle; 2D/3D asset pipeline;
  permanent rampage-first confirmation; canonical repo (ADR-003); delivery architecture
  (ADR-004/005).
- `CANON_BUILD_DELTA`: placeholder art below 07-spec frame counts; 1 of 6 encounter cards;
  no Wanted/Rampage/Overdrive/Fan Fire/High Noon/contract systems (out of slice scope).
- v4.0.0 standalone baseline (130,485 B, ChatGPT Library `libfile_08bab9c166a0819195c530ee1c386db5`)
  still not present in this repo; still unreachable from this build environment (proxy 403 to
  Sites deployments, unretested this cycle since nothing changed).
- Next milestone per the build receipt: Sprint 2 (six-card encounter grammar, anti-repeat
  selection, score economy, medals/contracts/seed codes) — not started; slice playtest gates
  have only been run by this automated environment, not by humans, per the receipt's own caveat.
- No pull request has been opened for `claude/eager-dirac-qv4zf4` / the slice work. Owner action
  needed if that's expected to ship as a PR.

## This session

No code changes. This file is the only addition — a dated verification snapshot for the brain.
