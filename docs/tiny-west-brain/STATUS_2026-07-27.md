# Tiny West — status check-in, 2026-07-27

Scheduled routine status update for `khuffsphone/tinywest`. Branch:
`claude/eager-dirac-ej7aw9`.

## 1. Repo / slice health — no regressions

- Repo history unchanged: 2 commits, working tree was clean before this check-in.
- Rebuilt from source: `node slice/tools/sprites.mjs emit` then `node slice/tools/build.mjs`.
  Output is byte-identical to the 2026-07-17 build receipt —
  `slice/dist/tiny-west.html`, 106,552 bytes,
  SHA-256 `6580961cf51bfed087f63a89558103e7af434f6a3f9784d02006bbd65f720bba`.
  `git status` was clean after the rebuild (no generated-file drift).
- Ran the full local QA suite (Playwright was available this session, unlike several
  prior check-ins that skipped it for lack of a `node_modules`):
  - `tools/playtest.mjs` — smoke, determinism (`runDeterminismCheck` PASS, identical
    hashes), natural keyboard run to CLEAN GETAWAY (~24 s, $269 banked), 0 page errors.
  - `tools/qa2.mjs` — pay-car route reachable/completable ($837 banked), touch pads +
    determinism on a touch-device context, four viewports with no overflow, 20 reset
    cycles with no error growth.
  - `tools/qa3.mjs` — fork → pay car via exact injected input: PASS; pay-car route
    completion: PASS ($839 banked).
  All results match the 2026-07-17 build receipt's figures — the slice is stable and
  unregressed ten days on.

## 2. CONFLICT-002 filed — repo canonical status vs. live v6 build line

See `CONFLICT-002.md` (new this check-in). Summary: the canonical Drive Brain runs an
active "Tiny West: Iron Trail v6" build line that revoked this repo's 1.2 MB ceiling on
2026-07-19 and has a current gate artifact of ~4.46 MB. This repo has not absorbed that
and has had no commits since 2026-07-17. `NEEDS_DECISION` (owner-level): is this repo
still canonical, or a frozen prototype. No code or constants changed here as a result —
report only, per this repo's own conflict-handling rule.

This repo's automation is now formally registered as Brain lane `GIT`
(`112_COWORK_AUDIT_ALPHA312_ART11_SFX62_DSN21-22_AND_DISPATCH.md`, DEC-112-9), with two
owner calls attached directly to it (see below and CONFLICT-002.md).

## 3. Security — live credential exposure (verified directly, not relayed)

The Brain's own governance notes (`GOV-111`, `GOV-112`, both 2026-07-26) record that a
`.env.txt` file containing an ElevenLabs API key sits in a Drive-synced, Drive-indexed
folder outside this repo, and that the key is confirmed live and already surfaced in a
plain Drive keyword search result snippet — meaning exposure does not require anyone to
deliberately open the file.

This check-in independently confirmed the file is **still present and unchanged** as of
now (created/modified 2026-07-26T01:11 UTC, no newer activity in the Brain folder since
GOV-112 at 18:47 UTC yesterday): a metadata lookup on that file returned a content
snippet matching a well-formed `ELEVENLABS_API_KEY=...` line. The key value itself is
**not** reproduced here or anywhere in this repo, per the Brain's own standing rule that
no lane echoes or pastes credential material.

**This is unresolved and owner-actionable**, not something this repo or this routine can
fix:
1. Rotate the key again at the ElevenLabs console and explicitly revoke the previous
   value (rotation without revocation is not remediation).
2. Delete `.env.txt` from the Drive-synced folder and empty the Drive trash.
3. Store the replacement outside any Drive-synced path (e.g. a user environment
   variable), and repoint the SFX tooling at it.
4. Re-run a keyword search over the tree to confirm nothing else holds credential
   material.

All paid audio generation (voice and non-voice) in the Drive-side project is blocked
until this is done (`DEC-112-4`).

## 4. Process note — PR cadence, and why this check-in did not open a new PR

This scheduled routine has opened roughly 100 near-duplicate "status check-in" PRs since
2026-07-25 (all targeting `claude/sunset-riders-clone-lyyefk`), with **zero merges** —
each run starts from a fresh scheduler-generated branch with no memory of prior runs, so
nothing consolidates. Several prior check-ins already flagged this (e.g. PR #95, #98),
and the Brain's own external audit independently flagged the identical problem
(`112_COWORK_AUDIT...DISPATCH.md` §7, "Owner call 2") with an explicit instruction to
the `GIT` lane: **"Do not open further duplicate PRs until the cadence is resolved."**

Per that instruction, this check-in pushed its branch and this status update but
**did not open PR #101**. Two open PRs already exist unmerged from prior runs (#99, #100).
Recommended owner action (unchanged from prior check-ins and the Brain audit): pick one
PR to merge as the consolidated baseline, close the rest of the backlog, and drop the
routine's cadence (daily or event-triggered instead of hourly) so future runs have
something to build on instead of restarting from zero each time.

## 5. Open items for the owner (nothing else changed here)

- **Security (most urgent):** rotate/revoke the ElevenLabs key and move it off the
  Drive-synced path (§3).
- **CONFLICT-002:** rule on this repo's canonical status vs. the v6 build line (§2).
- **PR cadence:** merge one consolidated status PR, close the rest, reduce schedule
  frequency (§4).
