# Status update — 2026-07-28

Repo: `khuffsphone/tinywest` · Branch checked: `claude/eager-dirac-n3rcy1`
(also present: `claude/sunset-riders-clone-lyyefk`, the branch the slice was
originally built on). Working tree clean at check time; no uncommitted
changes found before this update.

## Repo state

- 2 commits total: `cfb0034` (Sunset Riders tribute, `index.html`) and
  `a4c4dd4` (Rampage Express 42-second vertical slice, `slice/`). No commits
  since the 2026-07-17 build recorded in `BUILD_RECEIPT_SLICE.md`.
- **No open or merged pull request exists yet** for either
  `claude/eager-dirac-n3rcy1` or `claude/sunset-riders-clone-lyyefk` against
  this repo. The slice work is pushed to a branch but has not been proposed
  for merge to a default branch.

## Slice re-verification (this check)

Re-ran the full local test suite against the existing build, no code changes:

- `node tools/build.mjs` → `dist/tiny-west.html` rebuilt: **106,552 bytes**,
  SHA-256 `6580961cf51bfed087f63a89558103e7af434f6a3f9784d02006bbd65f720bba`
  — **byte-identical** to the 2026-07-17 build receipt. Build is reproducible.
- `node tools/playtest.mjs` → smoke PASS, `runDeterminismCheck` PASS (hash
  `911533983` both runs), full natural keyboard run reached `results` state
  cleanly (~24s, $269 banked), 0 page errors.
- `node tools/qa2.mjs` → pay-car route PASS, touch-device determinism PASS,
  all 4 viewports no clipping/overflow, 20 reset cycles bounded, 0 errors.
- `node tools/qa3.mjs` → pay-car fork reachable and completable via exact
  input ($839 banked), 0 errors.

All four gates green. No regressions since the audited 2026-07-17 build.

**Infra note:** `slice/tools/playtest.mjs` imports the `playwright` npm
package, but no `package.json`/lockfile is checked into the repo, so a clean
checkout has no way to install it deterministically. It was installed
locally and transiently for this check (against the pre-installed Chromium
at `/opt/pw-browsers`), then removed again before commit — nothing new is
tracked. Worth an owner decision: commit a `slice/package.json` pinning
`playwright`, or document the exact install step, so `docs/tiny-west-brain`'s
"Build & test" instructions are runnable from a fresh clone without
guessing a version.

## Open items (unchanged from Brain reconciliation)

Still `NEEDS_DECISION` at owner level, per `RECONCILIATION.md`: final public
subtitle, long-term 2D/3D asset pipeline (ADR-002/006), canonical repo
(ADR-003), delivery architecture / release target (ADR-004/005), formal v2
canon consolidation, asset-approval receipts.

- v4.0.0 baseline (130,485 B, ChatGPT Library `libfile_08bab9c166a0819195c530ee1c386db5`)
  still not present in this repo; still unreachable from this build
  environment (proxy blocks the Sites deployments). Not re-attempted this
  check — no new information.
- `CANON_BUILD_DELTA` stands as recorded: placeholder art below 07-spec frame
  counts, 1 of 6 encounter cards shipped, no Wanted/Rampage/Overdrive/Fan
  Fire/High Noon/contract systems (those are full-run scope, not slice scope).
- Sprint 2 (six-card grammar, anti-repeat selection, score economy,
  medals/contracts/seed codes) has not been started.

## Recommendation

The 42-second slice is stable and passing all recorded gates unchanged since
audit. The main gap is process, not code: nothing has been opened as a PR,
so the work sits on a branch with no review trail. Suggest opening a PR for
`claude/eager-dirac-n3rcy1` against the repo's default branch so the slice
gets a mergeable, reviewable home; flagging the `playwright` packaging gap to
the owner separately.
